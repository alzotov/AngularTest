'use strict';

describe('myApp.issuers module', function() {

  beforeEach(module('myApp.issuers'));

  describe('issuers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var issuersCtrl = $controller('IssuersCtrl');
      expect(issuersCtrl).toBeDefined();
    }));

  });
});